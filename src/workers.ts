const NUM_WORKERS = 2;

const workers = new Array<Worker>();

// for (let i = 0; i < NUM_WORKERS; i++) {
//    workers.push(new Worker("./collision-worker.ts"));
// }

export function startWorker(): void {

}